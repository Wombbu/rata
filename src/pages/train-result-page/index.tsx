/*import * as React from 'react';
import * as ReactRedux from 'react-redux';
import {RootState} from '../../state'
import {ResultPageWrapper} from '../../components/pages';

const TrainResultPage = (props: {}) => 
  <ResultPageWrapper>
  </ResultPageWrapper>;

const mapStateToProps = (state: RootState) => ({

});

const mapDispatchToProps = (dispatch: ReactRedux.Dispatch<any>) => ({

});
export default ReactRedux.connect(mapStateToProps, mapDispatchToProps)(TrainResultPage);
*/